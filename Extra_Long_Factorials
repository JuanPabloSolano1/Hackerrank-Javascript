let number = 25

let check = Array.from(Array(number + 1).keys())
let remove = check.shift()
let new_array = (check.sort((a,b) => (b-a)))

let count = number
for (var i = 0; i < new_array.length -1; i++) {
   count *= new_array[i + 1]
}
console.log(count)
